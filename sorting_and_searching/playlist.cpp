#include <bits/stdc++.h>
using namespace std; 

using ll = long long; 

int main(){
		ios::sync_with_stdio(false); 
		cin.tie(nullptr); 
		
		map<ll, ll> m; 

		ll n; cin >> n; 
		ll a[n], cnt = 0; 
		for(auto &i : a){
				cin >> i; 
		}
		for(int i = 0; i < j )

		return 0; 
}
